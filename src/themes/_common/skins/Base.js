import {css} from 'styled-components';

export const Container = {
    getDefaultStyle: (theme) => css`
          color: red;
          font-weight: 800;
          padding-bottom: 10px;
          padding-top: 35px;
    `
}

